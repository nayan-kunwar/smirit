{
  "name": "@smriti/postgres",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "vitest run --passWithNoTests",
    "migrate": "tsx src/bin/migrate.ts"
  },
  "dependencies": {
    "@smriti/config": "workspace:*",
    "@smriti/memory-core": "workspace:*",
    "@smriti/shared-types": "workspace:*",
    "kysely": "^0.27.5",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10"
  },
  "nx": {
    "tags": [
      "type:infra"
    ]
  }
}
